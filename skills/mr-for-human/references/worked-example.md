# 示例：给任务提交增加重试查重

这是合成的教学变更，下面包含完整的入口函数 before/after。它不是生产系统或真实 MR。代码块内的 `submit.py` 是临时验证仓库中的文件名；实际分析必须使用真实文件、SHA 和行号。

本示例按 [output-template.md](output-template.md) 的章节顺序组织，用来演示金字塔表达和底层四句式。

## 0. 对象与依赖约定

> 这次改动给 submit 增加 request_id 查重。请说明最值得看的设计、主流程和底层约束，让我能定位“重试后提示已接受，但任务没有执行”的问题。

教学需求：ACCEPTED 表示系统已经建立可恢复的任务执行责任；相同租户、同一 request_id 的串行重试返回原任务。不同租户不能通过该键取得其他租户的结果。

依赖约定：actor 来自可信认证入口；store 的 get/insert 在本教学模型中每次成功，记录在调用之间保留；broker.publish 可能在消息入队前抛出异常。模型没有后台恢复扫描。这里不规定真实数据库的持久化、并发唯一性或 broker 在响应丢失时的语义。

### Before — submit.py

```python
def submit(store, broker, actor, request):
    if not actor["can_submit"]:
        raise PermissionError("FORBIDDEN")
    job = store.insert(
        actor["tenant_id"], request["request_id"], request["payload"]
    )
    broker.publish(job["id"])
    return {"status": "ACCEPTED", "job_id": job["id"]}
```

### After — submit.py

```python
def submit(store, broker, actor, request):
    if not actor["can_submit"]:
        raise PermissionError("FORBIDDEN")
    existing = store.get(actor["tenant_id"], request["request_id"])
    if existing is not None:
        return {"status": "ACCEPTED", "job_id": existing["id"]}
    job = store.insert(
        actor["tenant_id"], request["request_id"], request["payload"]
    )
    broker.publish(job["id"])
    return {"status": "ACCEPTED", "job_id": job["id"]}
```

下文的行号均指 After 版本：权限检查第 2–3 行，查重第 4–6 行，插入第 7–9 行，发布第 10 行，返回第 11 行。

## 1. 第一屏

```
【明显问题】重试命中查重后直接返回 ACCEPTED，不再尝试发布，任务永不执行 — submit.py:4-6 与 7-10
  为什么排在这里：已能构造反例，且后果不可观测——外部看起来完全正常，没人会去修。

【潜在风险】同一 request_id 配不同 payload 时不比对内容，直接返回旧任务 — submit.py:4-6
  为什么排在这里：需求没有定义同键不同意图该怎么办，要先确认业务上会不会出现，不能从代码推。

【核心决定】幂等键的租户取自可信 actor 而非请求体，且权限检查在任何业务读写之前 — submit.py:2-4
  为什么排在这里：它承载了跨租户隔离这个承诺，是本次值得保留的实现选择。
```

## 2. 顶层：功能、抽象与契约

### 功能清单

| 触发 | 条件 | 成功结果 | 失败/拒绝 | 证据 |
|---|---|---|---|---|
| 已认证用户提交任务 | `actor.can_submit` 为真 | 返回 ACCEPTED 与 job_id | 无权限抛 FORBIDDEN；发布失败向调用者抛出 | 第 2–3、6、10–11 行 |

### 抽象清单

| 抽象 | 隐藏了哪个设计决定 | 调用者因此不必知道什么 | 边界内可替换什么 |
|---|---|---|---|
| `submit` 作为 facade | 任务如何被登记、去重和投递 | store 与 broker 的存在和调用顺序 | 登记与投递的内部步骤 |
| `store` / `broker` 作为参数（seam） | 存储与消息实现 | 具体存储引擎和消息中间件 | 可替换实现、可用测试替身驱动 |

这个 facade 确实减少了调用者要协调的步骤，但**没有封装完整责任**：它对外承诺“已接受”，而在失败路径上这个承诺没有兑现（见 4.4）。seam 的边界也只覆盖到接口形状——示例约定 store 每次成功，所以真实存储的持久化语义和并发唯一性不在这个 seam 的验证范围内。

### 契约与不变量

| ID | 性质 | 机制与证据 | 边界 |
|---|---|---|---|
| I01 | 无权限调用在业务读写前被拒绝 | 第 2–3 行，P01 | 上游 actor 的真实性由示例约定提供 |
| I02 | 相同租户/键的串行重试复用任务 | 第 4–6 行，P02 | 无并发唯一性或同键不同意图保证 |
| I03 | ACCEPTED 已建立可恢复执行责任 | 第 6 行与第 7–10 行之间存在矛盾 | 给定依赖模型中可构造反例；真实系统先查恢复机制 |

## 3. 中间层：执行逻辑

三个核心执行决定：

1. **按可信租户和 request_id 查重。**串行重试复用旧任务，不同租户使用同一个键仍可创建自己的任务。
2. **现有记录被当成 ACCEPTED 的充分条件。**新增早返回分支直接返回成功，没有检查任务是否已经发布。这是本次最重要的行为变化。
3. **插入与发布是两个独立步骤。**发布抛错后记录还在；重试进入新早返回分支，此后不再发布。

主链路：可信 actor → 权限检查 → 同租户查重 → 命中则返回旧任务；未命中则插入 → 发布 → 返回。

失败链路：插入成功 → 发布抛错 → 首次调用抛错 → 相同请求重试 → 命中记录 → 返回 ACCEPTED → broker 中仍没有任务。

原版同样有插入后发布失败的窗口，但重试会再次创建并发布；本次新增查重分支改变了后续行为。不能把整个失败窗口都说成本次新引入，也不能因此忽略早返回造成的新后果。

### 核心伪代码（当前实现）

```text
P01 授权
  若 actor.can_submit 为 false：抛出 FORBIDDEN

P02 查重
  existing = store.get(actor.tenant_id, request.request_id)
  若 existing 存在：返回 ACCEPTED(existing.id)
  // 没有检查发布状态，也没有核对同键 payload 是否相同

P03 创建并尝试发布（不是一个原子操作）
  job = store.insert(actor.tenant_id, request.request_id, request.payload)
  调用 broker.publish(job.id)
  // 这里抛错会向调用者传播；已插入记录仍在
  返回 ACCEPTED(job.id)
```

## 4. 底层：六个维度的实现说明

### 4.1 资源生命周期

这次实现只分配一种长期资源：store 中的任务记录（第 7–9 行）。函数内没有连接、文件或监听器需要释放，也没有为记录设置过期或清理路径。依赖的是有别的组件会消费或清理这些记录。证据：第 7–9 行，以及约定中“模型没有后台恢复扫描”。依赖不成立，所以 publish 失败留下的记录会永久存在，并且不会被任何组件消费。

### 4.2 规模与容量

每次调用一次 `get`（第 4 行）、最多一次 `insert`（第 7–9 行）、最多一次 `publish`（第 10 行）。没有循环、没有批量加载、没有 N+1，payload 原样传递不复制。工作量与业务数据规模无关，是常数。依赖的是 payload 自身大小由上游限制。证据：第 4–10 行。

这个维度的实现没有问题，但仍然写出来——“工作量是常数、没有全量加载”本身就是全貌的一部分。payload 是否有上限，示例没有给出信息，放入第 6 节。

### 4.3 并发一致性

查重是“先读后写”的两步（第 4 行 `get`，第 7 行 `insert`），中间没有唯一约束、没有条件更新、没有锁。依赖的是同一租户同一键的调用是串行的。证据：第 4–9 行。依赖不成立时，两个并发请求可能都读到空、都插入，产生两条记录和两次发布。

教学验证只覆盖串行重试，并发未执行；因此这里是**静态推演**，不是已验证结论。

### 4.4 部分失败

`store.insert`（第 7–9 行）和 `broker.publish`（第 10 行）是两个独立调用，不在同一事务或原子操作内，中间存在一个失败窗口。依赖的是两者之一成立：`publish` 不会失败，或者有恢复机制重新投递。证据：第 7–10 行，以及约定“publish 可能在消息入队前抛出异常”“模型没有后台恢复扫描”。两个依赖都不成立，所以会出现记录存在而消息不存在的状态；叠加新增的查重分支后，重试也不再尝试发布。

**这是本次最核心的实现事实**，也是第一屏第一条的来源。

### 4.5 信任边界

权限检查是函数做的第一件事（第 2–3 行），发生在任何业务读写之前。租户 ID 从 `actor` 取（第 4、8 行），不从 `request` 取，因此调用者无法通过请求体指定别人的租户。依赖的是 actor 来自可信认证入口。证据：第 2–4、8 行，约定给出 actor 可信。

这个维度的实现成立，是本次值得保留的选择。验证记录中“无权限提交”“不同租户使用相同键”两个场景实际执行并符合预期。

### 4.6 可观测与恢复

命中查重返回的 ACCEPTED（第 6 行）和新建任务返回的 ACCEPTED（第 11 行）结构完全相同，没有任何字段区分两者；函数不写日志，除 job_id 外不返回状态。依赖的是调用方或运维有其他手段判断任务是否已投递。证据：第 6、11 行。依赖不成立，所以任务静默丢失时系统从外部看完全正常——这解释了原始症状为什么难定位。

### 触发式维度

**幂等与重试身份**（适用）：幂等键是 `(tenant_id, request_id)`，作用域包含租户，这一点正确。但不比对 payload，同键不同意图会返回旧任务；去重记录没有 TTL，永不过期。证据：第 4–6 行。同键不同意图的处理方式属于契约问题，需求未定义。

**不适用的维度**：本次没有改超时/重试/池配置（无过载与背压维度）；没有时间计算、TTL 判断或时间戳排序（无时间与时钟维度）；没有缓存；没有改协议字段或编解码；没有数据库迁移；`publish` 之外没有不可逆外部操作，而 `publish` 的重复投递语义由 broker 决定，示例未规定。

## 5. 正向映射与未展开的改动

| 解释 | 教学源码位置 | 性质 |
|---|---|---|
| P01 / I01 / 4.5 | After 第 2–3 行 | 未改上下文，支撑信任边界 |
| P02 / I02 / D01 查重决定 / 4.3 / 4.6 | After 第 4–6 行 | 新增的查重分支 |
| P03 / I03 / 4.1 / 4.4 | Before 第 4–8 行；After 第 7–11 行 | 未改主链路，与新分支共同解释失败行为 |

**未展开的改动**：无。本次全部变化是 After 第 4–6 行的 3 行新增，已在上面展开。五类高敏感项逐类核对——权限声明未变（第 2–3 行是未改上下文）、无配置或默认值变化、无数据库迁移、无协议/IDL 定义、无共享工具函数。

**所有改动都已展开，不代表实现满足 I03。**记账说明的是“有没有讲到”，正确性由 4.4 的反例判断，两者分开报告。

## 6. 待确认

- 同一 request_id 配不同 payload 时应当拒绝、覆盖还是返回旧任务？需求未定义，属于契约问题。
- payload 是否有大小上限？示例未给出，因此 4.2 无法得出容量结论。
- 真实 broker 在响应丢失时的语义，以及真实 store 的持久化与并发唯一性保证。
- 并发下的双插入是静态推演，未执行验证。

## 7. 人的阅读与定位

从 After 第 4 行看查重使用的租户和 request_id；在第 6 行确认重试是否提前返回；到第 10 行检查 publish 是否被执行。观察 store 中有无任务、broker 中有无对应 ID，以及两次调用分别抛错还是返回 ACCEPTED。

对给定症状的回答：首次发布前失败留下记录；第二次调用提前返回，使发布不再尝试。首先定位查重早返回和发布之间的关系，再核对真实 store/broker/恢复器的保证。

### 判断和替代方案

已成立的好设计是从可信 actor 取得租户，并在读取历史结果之前检查权限。需要重点处理的是早返回的成功语义。

可选方案按实际环境比较：若已有可靠恢复扫描，核实其记录条件和责任即可；如果当前 API 允许改变契约，可返回“已记录、执行待确认”等真实状态；如果确实要求可靠接受且没有现有机制，再评估事务记录待发送意图及后续交付。直接在命中记录时重复 publish 可能制造重复执行，需先确认 broker/消费端的幂等边界。

### 验证边界

正式输出需把上述教学位置替换为固定 base/head 的源码链接；不能为本示例虚构 MR URL 或生产 commit。本示例的 Python 验证只能证明给定教学模型的行为，不能作为数据库、消息队列、生产故障或 Skill 自动触发稳定性的验证。真实运行示例时的固定 SHA 和结果记录于仓库综述配套的验证记录。
